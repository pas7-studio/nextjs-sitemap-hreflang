---
"nextjs-sitemap-hreflang": minor
---

Add production release automation (Changesets + GitHub Actions publish workflow), introduce `createSitemapEntriesFromManifest` for source-agnostic manifest integration, and rewrite README with badges, CI/release setup, and integration recipes.
