---
"@pas7/nextjs-sitemap-hreflang": minor
---

Add CI-grade check UX: stable `--json` report (`ok`, `issues`, `summary.byCode`, `inputPath`, `timingMs`), human suggestions per issue code, deterministic auto-detect priority with `--prefer <public|out|root>`, and explicit exit codes (0/2/4/5).
