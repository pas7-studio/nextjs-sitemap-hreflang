---
"@pas7/nextjs-sitemap-hreflang": minor
---

Improve Next.js MetadataRoute compatibility by accepting optional hreflang values, add `NextMetadataSitemapEntryCompatible`, extend `routingPAS7` with `suffixPaths` and `prefixPaths` (with deterministic priority), and refresh README for scoped package usage and static export integration.
