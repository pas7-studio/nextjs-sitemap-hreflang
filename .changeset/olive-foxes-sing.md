---
"@pas7/nextjs-sitemap-hreflang": minor
---

Improve DX for static export and mixed routing by adding CLI sitemap auto-detection (`public/sitemap.xml`, `out/sitemap.xml`, `sitemap.xml`), expanding App Router full-stack documentation, and adding a runnable `examples/next-app-router-static-export` project.
