---
"@pas7/nextjs-sitemap-hreflang": patch
---

Add `--prefer <public|out|root>` CLI option to control sitemap auto-detect priority for `check` and `inject` while keeping backward-compatible default behavior.
